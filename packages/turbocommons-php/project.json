{
  "name": "turbocommons-php",
  "root": "packages/turbocommons-php",
  "sourceRoot": "packages/turbocommons-php/src/main/php",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "composer install --no-interaction --no-progress && composer run build", "cwd": "packages/turbocommons-php" },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "composer install --no-interaction --no-progress && composer test", "cwd": "packages/turbocommons-php" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "composer install --no-interaction --no-progress && composer lint", "cwd": "packages/turbocommons-php" }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": { "command": "composer run package", "cwd": "packages/turbocommons-php" },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
