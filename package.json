{
  "name": "@turbocommons/monorepo",
  "private": true,
  "description": "Polyglot TurboCommons monorepo orchestration",
  "license": "Apache-2.0",
  "scripts": {
    "build": "docker compose run --rm --build toolbox build",
    "test": "docker compose run --rm --build toolbox test",
    "lint": "docker compose run --rm --build toolbox lint",
    "package": "docker compose run --rm --build toolbox package",
    "ci": "docker compose run --rm --build toolbox ci"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "jsdom": "^26.1.0",
    "nx": "^21.4.0",
    "qunit": "^2.24.1",
    "ts-loader": "^9.5.2",
    "typescript": "^5.9.2",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1"
  }
}
