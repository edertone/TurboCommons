{
  "name": "@turbocommons/monorepo",
  "private": true,
  "description": "Polyglot TurboCommons monorepo orchestration",
  "license": "Apache-2.0",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": "22.23.2",
    "npm": "10.9.8"
  },
  "scripts": {
    "build": "docker compose -f docker-compose.yaml run --rm --build toolbox build",
    "test": "docker compose -f docker-compose.yaml run --rm --build toolbox test",
    "test:java": "docker compose -f docker-compose.yaml run --rm --build toolbox nx run turbocommons-java:test",
    "test:php": "docker compose -f docker-compose.yaml run --rm --build toolbox nx run turbocommons-php:test",
    "test:shell": "docker compose -f docker-compose.yaml run --rm --build toolbox nx run turbocommons-shell:test",
    "test:ts": "docker compose -f docker-compose.yaml run --rm --build toolbox nx run turbocommons-ts:test",
    "lint": "docker compose -f docker-compose.yaml run --rm --build toolbox lint",
    "package": "docker compose -f docker-compose.yaml run --rm --build toolbox package",
    "ci": "docker compose -f docker-compose.yaml run --rm --build toolbox ci",
    "clean": "docker compose -f docker-compose.yaml run --rm --build toolbox clean",
    "dist": "docker compose -f docker-compose.yaml run --rm --build toolbox dist"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "jest": "30.4.2",
    "jest-environment-jsdom": "30.4.1",
    "nx": "21.6.11",
    "ts-loader": "9.6.2",
    "typescript": "5.9.3",
    "webpack": "5.109.2",
    "webpack-cli": "6.0.1"
  }
}
